import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6C63FF",
    },
    background: {
      default: "#F6F7FB",
    },
  },
  shape: {
    borderRadius: 12,
  },
});

export default theme;
